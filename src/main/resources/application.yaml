spring:
  application:
    name: cs203
  datasource:
    url: jdbc:mysql://localhost:3306/tariff
    username: root
    password: password123
  jwt:
    secret: ${JWT_SECRET}
    accessTokenExpiration: 300 # 5 minutes
    refreshTokenExpiration: 604800 # 7 days
  profiles:
    active: dev