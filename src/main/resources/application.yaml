spring:
  application:
    name: cs203
  datasource:
#    url: ${DB_URL}
#    username: ${DB_USERNAME:dev_user}
#    password: ${DB_PASSWORD}

    url: jdbc:mysql://localhost:3306/tariff?createDatabaseIfNotExist=true
    username: root   # Default value as fallback
    password: password123
  jpa:
    show-sql: true
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl