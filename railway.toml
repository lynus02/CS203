[build]
builder = "maven"
buildCommand = "mvn clean package -DskipTests"

[deploy]
startCommand = "java -jar target/cs203-*.jar --spring.profiles.active=prod"

[deploy.variables]
SPRING_PROFILES_ACTIVE = "prod"